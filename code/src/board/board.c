#include <game.h>
#include <board.h>

Board print_board(Board board)
{
    /*
    for (int i=0; i<board.size_x; i++)
    {
        for (int j=0; j<board.size_y; i=j++)
        {
            //printf("%c", board)
        }
    }
    */
}